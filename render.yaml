services:
  - type: web
    name: hmyut-web
    runtime: docker
    plan: starter
    healthCheckPath: /api/health
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: Download__RootPath
        value: /data/downloads
      - key: Download__FFmpegFilePath
        value: /usr/bin/ffmpeg
    disk:
      name: hmyut-downloads
      mountPath: /data/downloads
      sizeGB: 1
